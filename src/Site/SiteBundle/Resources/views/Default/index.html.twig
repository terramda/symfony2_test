{% extends '::base.html.twig' %}
{% block body %}
    <form action="{{ path('SiteSiteBundle_homepage') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        {{ form_rest(form) }}
        <input type="submit" value="Save" class=""/>
    </form>
{% endblock %}
